# 组件

React 组件库，提供可复用的 UI 组件和业务组件，覆盖常见的前端开发场景。

## 简介

本部分包含项目中常用的 React 组件，包括基础 UI 组件、业务组件、表单组件等。这些组件经过生产环境验证，可直接在项目中使用。

## 组件分类

### 基础组件

提供基础的 UI 元素，如按钮、输入框、选择器等。

### 业务组件

针对特定业务场景封装的组件，如支付组件、商品展示组件等。

### 表单组件

用于表单场景的组件集合，支持数据验证、联动等功能。

## 状态

> ⚠️ **即将更新**：文档内容正在完善中，敬请期待。
